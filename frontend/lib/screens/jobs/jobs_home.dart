import 'package:flutter/material.dart';

class JobsHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Jobs Home'),
      ),
      body: Center(
        child: Text('Welcome to the Jobs Home Page!'),
      ),
    );
   }
 }