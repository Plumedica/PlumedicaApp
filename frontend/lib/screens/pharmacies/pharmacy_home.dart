import 'package:flutter/material.dart';

class PharmacyHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pharmacy Home'),
      ),
      body: Center(
        child: Text('Welcome to the Pharmacy Home Page!'),
      ),
    );
   }
 }