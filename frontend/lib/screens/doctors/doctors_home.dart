import 'package:flutter/material.dart';

class DoctorsHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Doctors Home')), body: Center(child: Text('Doctors Home Page')));
  }
}
// Repeat for each screen you need.
