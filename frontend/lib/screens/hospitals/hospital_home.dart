import 'package:flutter/material.dart';

class HospitalHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hospital Home'),
      ),
      body: Center(
        child: Text('Welcome to the Hospital Home Page!'),
      ),
    );
   }
 }  